apply plugin: 'maven-publish'

afterEvaluate {
    publishing {
        repositories {
            maven {
                name = 'GitHubPackages'
                url = uri('https://maven.pkg.github.com/HTMLgtMK/LifecycleAwareViewWidget')
                credentials {
                    username = project.findProperty('gpr.user') ?: System.getenv('GITHUB_ACTOR')
                    password = project.findProperty('gpr.key') ?: System.getenv('GITHUB_TOKEN')
                }
            }
        }
        publications {
            release(MavenPublication) {
                from components.release

                groupId = project.PUBLISH_GROUP_ID
                artifactId = project.PUBLISH_ARTIFACT_ID
                version = project.PUBLISH_VERSION

                pom {
                    name = 'CleanAdapter'
                    description = 'A clean RecyclerView adapter library with ViewBinding support and optional Paging 3 integration.'
                    url = 'https://github.com/HTMLgtMK/LifecycleAwareViewWidget'
                    licenses {
                        license {
                            name = 'The Apache License, Version 2.0'
                            url = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                        }
                    }
                }
            }
        }
    }
}
